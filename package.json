{
  "name": "zidioconnect-root",
  "version": "1.0.0",
  "description": "Root package.json for ZidioConnect - Redirects to client directory",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "cd client && npx vite preview --host 0.0.0.0 --port ${PORT:-3000}",
    "install": "cd client && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}

